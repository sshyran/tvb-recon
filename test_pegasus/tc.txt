tr mri_convert {
    profile env "FREESURFER_HOME" "${FREESURFER_HOME}"

    site condorpool {
        pfn "/WORK/FS_NEW/freesurfer/bin/mri_convert"
        arch "x86_64"
        os "${OS}"
        type "INSTALLED"
    }
}

tr recon {
    profile env "FREESURFER_HOME" "${FREESURFER_HOME}"
    profile env "HOME" "${HOME}"

    site condorpool {
        pfn "/WORK/BNM/tvb-recon/tvb-recon/test_pegasus/recon.sh"
        arch "x86_64"
        os "${OS}"
        type "STAGEABLE"
    }
}

tr mrconvert {
    profile env "HOME" "${HOME}"

    site condorpool {
         pfn "/WORK/MRtrix/mrtrix3/release/bin/mrconvert"
        arch "x86_64"
        os "${OS}"
        type "INSTALLED"
    }
}

tr dwipreproc {
    profile env "HOME" "${HOME}"

    site condorpool {
        pfn "/WORK/MRtrix/mrtrix3/scripts/dwipreproc"
        arch "x86_64"
        os "${OS}"
        type "INSTALLED"
    }
}

tr dwi2mask {
    profile env "HOME" "${HOME}"

    site condorpool {
        pfn "/WORK/MRtrix/mrtrix3/release/bin/dwi2mask"
        arch "x86_64"
        os "${OS}"
        type "INSTALLED"
    }
}

tr dwiextract {
    profile env "HOME" "${HOME}"
    profile env "MRTRIX_BIN" "${MRTRIX_BIN}"

    site condorpool {
        pfn "/WORK/BNM/tvb-recon/tvb-recon/test_pegasus/dwiextract.sh"
        arch "x86_64"
        os "${OS}"
        type "INSTALLED"
    }
}

tr flirt {
    profile env "FSL_DIR" "${FSL_DIR}"

    site condorpool {
        pfn "/WORK/BNM/tvb-recon/tvb-recon/test_pegasus/flirt.sh"
        arch "x86_64"
        os "${OS}"
        type "STAGEABLE"
    }

}

tr convert-xfm {
    profile env "FSL_DIR" "${FSL_DIR}"

    site condorpool {
        pfn "/WORK/FSL/fsl/bin/convert_xfm"
        arch "x86_64"
        os "${OS}"
        type "INSTALLED"
    }
}

tr flirt-reversed {
    profile env "FSL_DIR" "${FSL_DIR}"

    site condorpool {
        pfn "/WORK/BNM/tvb-recon/tvb-recon/test_pegasus/flirt-reversed.sh"
        arch "x86_64"
        os "${OS}"
        type "STAGEABLE"
    }
}

tr ttgen {
    profile env "HOME" "${HOME}"
    profile env "FSLDIR" "${FSL_DIR}"
    profile env "MRTRIX_BIN" "${MRTRIX_BIN}"
    profile env "MRTRIX_SCRIPTS" "${MRTRIX_SCRIPTS}"

    site condorpool {
        pfn "/WORK/BNM/tvb-recon/tvb-recon/test_pegasus/ttgen.sh"
        arch "x86_64"
        os "${OS}"
        type "STAGEABLE"
    }
}

tr tt2gmwmi {
    profile env "HOME" "${HOME}"

    site condorpool {
        pfn "/WORK/MRtrix/mrtrix3/release/bin/5tt2gmwmi"
        arch "x86_64"
        os "${OS}"
        type "INSTALLED"
    }
}

tr tt2vis {
    profile env "HOME" "${HOME}"

    site condorpool {
        pfn "/WORK/MRtrix/mrtrix3/release/bin/5tt2vis"
        arch "x86_64"
        os "${OS}"
        type "INSTALLED"
    }
}

tr dwi2response {
    profile env "HOME" "${HOME}"
    profile env "MRTRIX_BIN" "${MRTRIX_BIN}"
    profile env "MRTRIX_SCRIPTS" "${MRTRIX_SCRIPTS}"

    site condorpool {
        pfn "/WORK/BNM/tvb-recon/tvb-recon/test_pegasus/dwi2response.sh"
        arch "x86_64"
        os "${OS}"
        type "STAGEABLE"
    }
}

tr dwi2fod {
    profile env "HOME" "${HOME}"

    site condorpool {
        pfn "/WORK/MRtrix/mrtrix3/release/bin/dwi2fod"
        arch "x86_64"
        os "${OS}"
        type "INSTALLED"
    }
}

tr tckgen {
    profile env "HOME" "${HOME}"

    site condorpool {
        pfn "/WORK/MRtrix/mrtrix3/release/bin/tckgen"
        arch "x86_64"
        os "${OS}"
        type "INSTALLED"
    }
}

tr tcksift {
    profile env "HOME" "${HOME}"

    site condorpool {
        pfn "/WORK/MRtrix/mrtrix3/release/bin/tcksift"
        arch "x86_64"
        os "${OS}"
        type "INSTALLED"
    }
}

tr tckmap {
    profile env "HOME" "${HOME}"

    site condorpool {
        pfn "/WORK/MRtrix/mrtrix3/release/bin/tckmap"
        arch "x86_64"
        os "${OS}"
        type "INSTALLED"
    }
}

tr labelconvert {
    profile env "HOME" "${HOME}"

    site condorpool {
        pfn "/WORK/MRtrix/mrtrix3/release/bin/labelconvert"
        arch "x86_64"
        os "${OS}"
        type "INSTALLED"
    }
}

tr tck2connectome {
    profile env "HOME" "${HOME}"

    site condorpool {
        pfn "/WORK/MRtrix/mrtrix3/release/bin/tck2connectome"
        arch "x86_64"
        os "${OS}"
        type "INSTALLED"
    }
}

tr convert_output {
    profile env "FREESURFER_HOME" "${FREESURFER_HOME}"

    site condorpool {
        pfn "/WORK/BNM/tvb-recon/tvb-recon/test_pegasus/convert_output.sh"
        arch "x86_64"
        os "${OS}"
        type "STAGEABLE"
    }
}

tr mri_pretess {
    profile env "FREESURFER_HOME" "${FREESURFER_HOME}"

    site condorpool {
        pfn "/WORK/FS/freesurfer//bin/mri_pretess"
        arch "x86_64"
        os "${OS}"
        type "INSTALLED"
    }
}

tr mri_tessellate {
    profile env "FREESURFER_HOME" "${FREESURFER_HOME}"

    site condorpool {
        pfn "/WORK/FS/freesurfer//bin/mri_tessellate"
        arch "x86_64"
        os "${OS}"
        type "INSTALLED"
    }
}

tr mris_extract_main_component {
    profile env "FREESURFER_HOME" "${FREESURFER_HOME}"

    site condorpool {
        pfn "/WORK/FS/freesurfer//bin/mris_extract_main_component"
        arch "x86_64"
        os "${OS}"
        type "INSTALLED"
    }
}

tr mris_smooth {
    profile env "FREESURFER_HOME" "${FREESURFER_HOME}"

    site condorpool {
        pfn "/WORK/FS/freesurfer//bin/mris_smooth"
        arch "x86_64"
        os "${OS}"
        type "INSTALLED"
    }
}

tr aseg_concatenation {
    profile env "FREESURFER_HOME" "${FREESURFER_HOME}"
    profile env "SUBJECT" "${SUBJECT}"

    site condorpool {
        pfn "/WORK/BNM/tvb-recon/tvb-recon/test_pegasus/aseg_concatenation.sh"
        arch "x86_64"
        os "${OS}"
        type "STAGEABLE"
    }
}

tr mris_convert {
    profile env "FREESURFER_HOME" "${FREESURFER_HOME}"

    site condorpool {
        pfn "/WORK/FS/freesurfer//bin/mris_convert"
        arch "x86_64"
        os "${OS}"
        type "INSTALLED"
    }
}

tr bbregister {
    profile env "HOME" "${HOME}"
    profile env "FSL_DIR" "${FSL_DIR}"
    profile env "FREESURFER_HOME" "${FREESURFER_HOME}"

    site condorpool {
        pfn "/WORK/BNM/tvb-recon/tvb-recon/test_pegasus/bbregister.sh"
        arch "x86_64"
        os "${OS}"
        type "STAGEABLE"
    }
}

tr mri_vol2vol {
    profile env "FREESURFER_HOME" "${FREESURFER_HOME}"

    site condorpool {
        pfn "/WORK/FS/freesurfer//bin/mri_vol2vol"
        arch "x86_64"
        os "${OS}"
        type "INSTALLED"
    }
}

tr qc_snapshot {
    profile env "FREESURFER_HOME" "${FREESURFER_HOME}"
    profile env "SUBJECT" "${SUBJECT}"

    site condorpool {
        pfn "/WORK/BNM/tvb-recon/tvb-recon/test_pegasus/qc_snapshot.sh"
        arch "x86_64"
        os "${OS}"
        type "STAGEABLE"
    }
}

tr gen_fs_custom {
    profile env "FREESURFER_HOME" "${FREESURFER_HOME}"
    profile env "SUBJECT" "${SUBJECT}"

    site condorpool {
        pfn "/WORK/BNM/tvb-recon/tvb-recon/test_pegasus/gen_fs_custom.sh"
        arch "x86_64"
        os "${OS}"
        type "STAGEABLE"
    }
}

tr autorecon3-t2 {
    profile env "HOME" "${HOME}"
    profile env "FREESURFER_HOME" "${FREESURFER_HOME}"

    site condorpool {
        pfn "/WORK/BNM/tvb-recon/tvb-recon/test_pegasus/autorecon3-t2.sh"
        arch "x86_64"
        os "${OS}"
        type "STAGEABLE"
    }
}

tr autorecon3-flair {
    profile env "HOME" "${HOME}"
    profile env "FREESURFER_HOME" "${FREESURFER_HOME}"

    site condorpool {
        pfn "/WORK/BNM/tvb-recon/tvb-recon/test_pegasus/autorecon3-flair.sh"
        arch "x86_64"
        os "${OS}"
        type "STAGEABLE"
    }
}